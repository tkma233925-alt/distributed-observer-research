---
name: claude
description: プロジェクト全体の背景・進め方・規約をまとめた Claude との協働マニュアル
---

# claude.md - 分散オブザーバ設計研究

## 📌 研究概要

**プロジェクト名**  
センサーネットワークの分散オブザーバ設計：重み付き協調ゲイン設計の論文執筆と数値検証

**研究テーマ**  
断続的な通信（ランダム通信）を持つセンサーネットワークにおける、重み付き協調ゲイン設計

**研究者**  
- こうた（今村耕太）：北海道大学大学院生
- 山下教授、小林教授：共著者・指導教授

**執筆形式**  
日本語 LaTeX、重み付き Lyapunov 関数アプローチを LMI 最適化問題として定式化

---

## 🎯 現在の進捗（Latest Status）

### ステージ
数値検証・図表精査段階

### 検証内容
- N=75 ノードシミュレーション（`examples_simulation_N75.ipynb`）
- 2つの仮説検証：
  1. 重み最適化が低次数ノードに利益をもたらすこと
  2. 重み最適化が周辺ノードに利益をもたらすこと

### 現在進行中のタスク
- 例2の時系列応答プロット（`all_nodes_timeresponse.pdf`）の精査
  - ノードを原点からの距離に基づいて3グループに分類
  - 色分け表示：黒（中心群）、青（中間群）、赤細線（周辺群=`exp2.focus_nodes`の22ノード）
  - 図の役割を「重み付けのもとでノードが発散しないことの確認」に再定義
  - MSE 改善の定量的根拠はテーブルに委ねる構成

---

## 🛠️ ツール・環境

| 用途 | ツール |
|------|--------|
| シミュレーション・可視化 | Jupyter Notebook |
| 計算言語 | Python（NumPy、Matplotlib） |
| 論文執筆 | 日本語 LaTeX |
| 最適化 | LMI ソルバー |
| 複数環境管理 | 一様確率・2地域確率・M地域設定の複数バージョン |

---

## 📚 理論上の重要な知見

### カーネル構造の区別（双方向通信の安定性解析）

$\Lambda_i$ 行列の構造が異なる：
- **平均項カーネル**：$(e_i - e_j)e_i^\top$
- **分散項カーネル**：$(e_i - e_j)(e_i - e_j)^\top$

**効果**  
このカーネル構造の修正により：
- $\sigma$ が約半減
- 利用可能な協調ゲイン $\epsilon$ が倍増

### $\mathbf{R}$ と $\bar{\mathbf{R}}_i$ の関係

$$\mathbf{R} = \sum_i \beta_i^{-1}(e_ie_i^\top \otimes \bar{R}_i)$$

- 数値的に検証済み
- $\beta_i$ の重みは $\mathbf{P}$ の定義を通じて $\mathbf{R}$ に反映される

---

## 📋 コード・図表の運用規約

### 図表・記述の誠実さ

- 図中に視覚的なグループ差が明確でない場合、「差が見える」と主張するLaTeX記述は採用しない
- 定量データ（MSEテーブル）に証拠の重点を置く
- 図の役割を明確に定義する（例：「発散しないことの確認」）

### 変数の整合性

- 図とテーブルが**同一のノード集合**（`exp2.focus_nodes`）を参照する
- コード内で独自定義せず、既存ノートブック変数を使用
- ノートブック変数・関数の再利用を優先：`df2`, `exp2.focus_nodes`, `save_fig`, `PDF_DIR` など

### np.argsort の符号規約

| 例 | 定義 | 符号 |
|-----|------|------|
| 例1 | 昇順（低次数） | デフォルト |
| 例2 | 降順（高距離） | `::-1` |

明示的に確認済み。コード中でコメント化して明記する。

---

## 🤝 Claude との協働方針

### サポート対象

#### 1. **理論導出のチェック**
- 数式の正確性確認
- 定理の証明フロー確認
- カーネル構造などの微妙な違いを検証

#### 2. **シミュレーションコード実装**
- Python コードのデバッグ
- NumPy・Matplotlib の活用法
- ノートブックの構造化・効率化

#### 3. **LaTeX 論文執筆**
- 章立て・構成の改善
- 数式環境の整備
- 日本語表現・論理の改善
- 図表のキャプション・説明文

#### 4. **図表の自動更新**
- シミュレーション結果の自動実行
- 数値データの抽出・テーブル化
- LaTeX への自動追記

### 進め方（Work Style）

- **並行進行スタイル**：理論導出・コードデバッグ・LaTeX修正・数値シミュレーションを並行して進める
- **密度の濃い共同作業**：教授のホワイトボード導出を起点に、数値検証で理論の正しさを確認
- **段階的な精緻化**：粗い結果 → 理論との照合 → 図表精査 → 論文作成

---

## 📂 ファイル構造

```
プロジェクトルート/
├── claude.md                          # このファイル（Claude との協働マニュアル）
├── index.md                            # プロジェクトの基本情報
├── overview.md                         # 論文の目的・体制・進捗状況
├── theory-and-conventions.md          # 理論・コード・図表の規約
├── paper/                              # LaTeX 論文ファイル
│   └── main.tex
├── code/
│   └── examples_simulation_N75.ipynb  # メイン検証ノートブック
└── figures/                            # 生成される図表（PDF/PNG）
    └── all_nodes_timeresponse.pdf
```

---

## 💬 よくある相談パターン

### パターン1：「このシミュレーション結果を論文に反映して」
```
→ Claude が実行すること：
  1. ノートブックを実行して最新の数値を抽出
  2. MSE・σ・ε などの値をテーブル化
  3. LaTeX の該当セクションに自動追記
  4. 図表を最新版で上書き
```

### パターン2：「このコード、もっと効率的に書けますか？」
```
→ Claude が確認すること：
  - 既存ノートブック変数との整合性
  - np.argsort の符号規約を遵守しているか
  - 関数再利用の可能性
```

### パターン3：「この理論導出、正しいですか？」
```
→ Claude が検証すること：
  - カーネル構造の定義が正確か
  - Lyapunov 関数の形式が適切か
  - LMI 変換に誤りがないか
```

---

## 🔗 関連プロジェクトファイル

| ファイル | 役割 |
|---------|------|
| `index.md` | プロジェクト概要 |
| `overview.md` | 論文の目的・体制・進捗 |
| `theory-and-conventions.md` | 理論・規約詳細 |

---

## ✅ 最後に

このドキュメントは研究プロジェクトの「生きたマニュアル」です。  
以下の場面で Claude に参照してもらうことで、より正確で効率的なサポートが得られます：

- 「claude.md の規約に従って…」
- 「theory-and-conventions.md を確認して…」
- 「現在の進捗（overview.md参照）を踏まえて…」

質問や新しい工夫が生まれたら、このドキュメントを更新してください。
